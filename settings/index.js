const config = {
  database: {
    drive: 'mysql' /* one of 'mysql' | 'mariadb' | 'postgres' | 'mssql' */,
    host: 'localhost',
    port: 3306,
    database: 'b2bcomm',
    user: 'tigenios', // <-- Verificar seu usuário de acesso
    password: 'melquiades' // <-- Verificar sua senha
  }
}
module.exports = config
